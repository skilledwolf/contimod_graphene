# Roadmap

This file is the shared backlog for `contimod_graphene` cleanup, packaging, validation, and API-shaping work. Add tasks here as soon as they are discovered. Update task status in the same unit of work when tasks start, finish, split, get blocked, or are no longer relevant.

## Clean Worktree Plan
- [x] Add repo-root `AGENTS.md` and `Roadmap.md` files so package-specific guidance and backlog tracking live in `contimod_graphene`, not only in downstream repos.
- [x] Reframe package-facing guidance and metadata so `contimod_graphene` is presented as a standalone package in its own right rather than as a helper package, while still documenting `contimod` as an important downstream integration.
- [x] Write `PUBLIC_API_PLAN.md` capturing the recommended standalone API, naming, JAX-compatible model workflow, parameter-management direction, and implementation order so follow-on agents can implement without needing prior chat context.

## Active Priorities
- [ ] Continue sharpening the standalone-package boundary: low-level Bernal/ABC kernels, parameter sets, basis/symmetry helpers, and lightweight single-particle utilities should live here, while `contimod` should keep discretization, mesh/state containers, and many-body workflows (SCF, susceptibility, TDHF, superconductivity) unless a reusable primitive clearly belongs downstream.
- [x] Tighten `README.md` so standalone `contimod_graphene` usage leads throughout, with any `contimod` mention reduced to a brief downstream note near the bottom. Completed (2026-03-30): removed the early README relationship section and intro emphasis on downstream integration, then reintroduced `contimod` only as a short bottom-of-page downstream note.
- [x] When the new `contimod_graphene` public API lands, migrate `/Users/wolft/Dev/contimod` to it in the same unit of work instead of adding compatibility shims here. `contimod` is currently the main downstream consumer, so direct migration is preferred over preserving stale call sites. Completed (2026-03-30): `src/contimod/materials/graphene.py` now caches `BernalMultilayer` / `RhombohedralMultilayer` instances and stores `GrapheneTBParameters` instead of raw preset dicts, while `tests/conftest.py` and graphene regression/import tests were updated to the new surface. Validation: `JAX_PLATFORMS=cpu pytest -ra /Users/wolft/Dev/contimod/tests/test_graphene_regression.py /Users/wolft/Dev/contimod/tests/test_hamiltonian_meshgrid_regression.py /Users/wolft/Dev/contimod/tests/test_import_surface.py`.
- [ ] Decide whether the package should keep the current distribution/import naming or adopt a more independent name. If a rename is chosen, define the migration strategy separately for the distribution name and the Python import path rather than changing both casually.
- [x] Implement the higher-level model surface described in `PUBLIC_API_PLAN.md`, centered on `BernalMultilayer` / `RhombohedralMultilayer` and a thin wrapper layer over the existing low-level kernels. Completed (2026-03-30): added `src/contimod_graphene/models.py` with immutable model wrappers, batch helpers, LL/two-band methods, and family/default metadata. Validation: `JAX_PLATFORMS=cpu python -m pytest -ra tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py`.
- [x] Design and land the explicit standalone public API layer described in `PUBLIC_API_PLAN.md` instead of exposing mostly module namespaces. Keep `bernal.py` / `rhombohedral.py` as low-level kernels on the first pass, add a small `models.py` or `api.py` surface with stable user-facing constructors, and make `src/contimod_graphene/__init__.py` export that surface intentionally. Validation: import-level API smoke tests plus docs/README examples updated to use the new entry points. Completed (2026-03-30): `src/contimod_graphene/__init__.py` now exports the parameter/model surface intentionally while still exposing the low-level modules for advanced users, and README/docs pages were rewritten to lead with the new top-level API.
- [x] Replace the raw-dict parameter surface with the stronger parameter-management layer described in `PUBLIC_API_PLAN.md`: preserve JSON-backed presets, but add validated overlay/update helpers and a stable public parameter object so users can inspect, copy, override, and serialize parameters without guessing which keys matter. Validation: focused tests for preset loading, alias resolution, overlay semantics, family validation, and round-tripping custom parameter files. Completed (2026-03-30): `GrapheneTBParameters` is now an immutable mapping-compatible JAX PyTree with preset loading, alias resolution, `.replace(...)`, JSON round-trips, `extras`, and family validation; built-in presets now use it directly. Validation: `JAX_PLATFORMS=cpu python -m pytest -ra tests/test_params.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py tests/test_symmetry.py`.
- [x] Remove duplicated low-level helpers and make module ownership obvious. Right now `src/contimod_graphene/utils.py` and `src/contimod_graphene/basis.py` both define layer/sublattice coordinate helpers; decide which module owns geometry/basis metadata versus numerical helper functions and collapse the duplicates behind one documented import path. Validation: update imports, remove dead helpers, and keep existing basis/LL tests green. Completed (2026-03-30): `basis.py` is now the canonical owner of layer/sublattice coordinate metadata, while `utils.py` re-exports that ownership and keeps only numerical helpers/batching.
- [x] Add standalone graphene symmetry helpers in `src/contimod_graphene/symmetry.py`, export them from `src/contimod_graphene/__init__.py`, and fix `src/contimod_graphene/basis.py` so `spin_op` / `valley_op` build actual Pauli operators rather than scalar multiples of the identity. Validation: `JAX_PLATFORM_NAME=cpu pytest -ra tests/test_symmetry.py` and `JAX_PLATFORM_NAME=cpu python -m pytest -ra tests/test_symmetry.py tests/test_rhombohedral.py tests/test_bernal.py`.
- [x] Fix the current model-identity mismatch in `src/contimod_graphene/rhombohedral.py`: rhombohedral defaults now target the ABC/trilayer preset, the convenience getters default to the trilayer entry point, `tests/test_rhombohedral.py` covers default-preset selection, and docs now call out both the intended ABC defaults and the intentional `n_layers=2` AB/ABC equivalence in the bilayer LL example. Validation: `JAX_PLATFORM_NAME=cpu pytest -ra tests/test_rhombohedral.py tests/test_bernal.py` plus `JAX_PLATFORM_NAME=cpu python -c "import contimod_graphene; print('import ok')"`; no `/Users/wolft/Dev/contimod` changes were required because current downstream callers already pass explicit rhombohedral parameters/layer counts.
- [x] Audit docs and examples so the default usage story stands on its own without assuming `contimod`, except for explicitly downstream-integration documents. Completed (2026-03-30): README/docs now lead with the standalone top-level API, `examples/standalone_quickstart.py` and `examples/standalone_gallery.py` provide maintained package-native entry points, `examples/README.md` classifies the example surface, `docs/generate_example_figures.py` reuses the gallery surface for the tracked docs assets, and `tests/test_docs_examples.py` validates the documented/example flow on CPU.
- [ ] Evaluate `ase` as an optional dependency for Brillouin-zone/path/plotting helpers if that produces a cleaner standalone single-particle API.
- [ ] Evaluate `spglib` as an optional dependency only if symmetry classification becomes part of the intended public surface.
- [ ] Resolve the current JAX-on-Metal validation gap surfaced during local testing: existing Hamiltonian and symmetry tests hit unsupported backend paths on Apple Metal (`stablehlo` bytecode attribute errors, `schur`, and `default_memory_space`), so CPU is the only reliable validation backend on this machine right now. Validation: reproduce the failures intentionally, then decide whether to document/pin CPU for tests or refactor the affected JAX/NumPy boundaries for backend portability.
- [ ] Strengthen validation beyond shape/hermiticity smoke tests for the core Hamiltonian builders, especially where the package intends to make standalone scientific claims. Progress (2026-03-31): the fast paper-backed validation tier now covers clean-subspace BLG exact/reduced checks, bilayer AB/ABC equivalence, inversion-protected LL valley degeneracy for Bernal bilayer/tetralayer/hexalayer and ABC trilayer/tetralayer/pentalayer, ABA trilayer mirror-parity decoupling plus clean monolayer-like / bilayer-like zero-field sectors, clean LL decomposition, full-parameter LL mirror decoupling, the exact full-parameter monolayerlike odd LL block in ABA trilayer, the exact source-aligned bilayerlike even LL block `H_2 = H(sqrt(2)) + W(1/2, 0)`, clean ABC LL zero-mode counting for `N=3,4,5`, and the ABAB four-layer zero-field `W/W'` parity check, along with ABC outer-site / low-energy-scaling checks via `tests/test_physics_validation.py`, `PHYSICS_VALIDATION_PLAN.md`, the zero-field basis helpers in `src/contimod_graphene/basis.py`, and the compact parameter-convention note in `docs/usage.md`; follow-on work still remains for ABA detailed bilayerlike low-energy pair tracking and slower LL/trigonal-warping regressions.
- [x] Land the exact full-parameter ABA trilayer monolayerlike LL block-content check before attempting slower family-structure regressions. Completed (2026-03-31): `tests/test_physics_validation.py` now verifies that the odd mirror block of the realistic ABA trilayer LL Hamiltonian matches the exact monolayerlike block `H_0` from the literature after translating the package parameters to `Delta_eff = Delta - gamma2` and `delta_eff = delta - (gamma2 + gamma5)/2`. Validation: `python -m py_compile tests/test_physics_validation.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py tests/test_docs_examples.py`.
- [x] Land the exact source-aligned ABA trilayer bilayerlike LL block-content check before attempting low-energy pair regressions. Completed (2026-03-31): `tests/test_physics_validation.py` now verifies that the mirror-basis even block of ABA trilayer matches the paper's exact `H_2 = H(sqrt(2)) + W(1/2, 0)` construction at `Delta=0`, by comparing it to a bilayer-like LL Hamiltonian with `gamma1`, `gamma3`, and `gamma4` scaled by `sqrt(2)` plus the residual diagonal `gamma2/gamma5` correction on `(A_+, B_+, A_2, B_2)`. Validation: `python -m py_compile tests/test_physics_validation.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py tests/test_docs_examples.py`.
- [ ] Resolve the remaining ABA trilayer low-energy bilayerlike-pair identification issue before adding slower family-structure regressions. Current blocker (2026-03-31): the source-backed review now fixes the paper-to-package valley map (`K -> xi = -1`, `K' -> xi = +1`) and the exact mirror-basis block ordering, but once `gamma3` is restored the bilayerlike `n = -1, 0` levels only survive as adiabatic descendants inside the full even block. The next slower regression therefore still needs explicit state tracking or a reduced `gamma3 = 0` observable rather than a fixed sorted-eigenvalue index.
- [x] Extend the exact rhombohedral validation wave to generic higher-`N` LL structure. Completed (2026-03-31): `tests/test_physics_validation.py` now adds exact pentalayer `K/K'` LL spectral degeneracy at `U=0`, `Delta=0`, extends the clean nearest-neighbor ABC outer-site `k=0` zero-mode test to `N=5`, and pins the exact clean-subset LL zero-mode count `N_zero = N_layers` for rhombohedral `N=3,4,5` in both valleys. `PHYSICS_VALIDATION_PLAN.md` and `docs/usage.md` now make the rhombohedral `Delta` / `delta` convention boundary more explicit for this stronger ABC-first validation tier. Validation: `python -m py_compile tests/test_physics_validation.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_rhombohedral.py tests/test_models.py tests/test_docs_examples.py`.
- [x] Extend the exact even-`N` Bernal LL valley-degeneracy coverage beyond tetralayer and add a compact parameter-convention note in the clean standalone docs. Completed (2026-03-31): `tests/test_physics_validation.py` now includes a custom Bernal `N=6` LL `K/K'` degeneracy regression at `U=Delta=0` using the full remote-hopping `4lg` parameter set, while `docs/usage.md` now spells out Bernal `Delta` vs `delta` and the package-specific rhombohedral `Delta` meaning for `N>3`. Validation: `python -m py_compile tests/test_physics_validation.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py tests/test_docs_examples.py`.
- [x] Fix the Bernal next-nearest-layer `W/W'` parity bug identified by the literature review. Completed (2026-03-31): `src/contimod_graphene/bernal.py` now alternates the `i -> i+2` next-nearest-layer block between `W = diag(gamma2/2, gamma5/2)` on odd-starting links and `W' = diag(gamma5/2, gamma2/2)` on even-starting links in both the zero-field and LL builders. `tests/test_bernal.py` now pins the ABAB four-layer `1 -> 3` versus `2 -> 4` block structure directly, and `tests/test_physics_validation.py` now covers exact `K/K'` LL degeneracy for the full Bernal `4lg` preset at `U=Delta=0`. Validation: `python -m py_compile src/contimod_graphene/bernal.py tests/test_bernal.py tests/test_physics_validation.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_bernal.py tests/test_physics_validation.py tests/test_models.py tests/test_rhombohedral.py`.
- [x] Land the first inversion-protected LL valley-degeneracy validation wave in the fast suite. Completed (2026-03-31): `tests/test_physics_validation.py` now verifies exact `K/K'` LL spectral degeneracy for Bernal bilayer and rhombohedral trilayer/tetralayer at `U=0` through the public model wrappers, and `PHYSICS_VALIDATION_PLAN.md` records the symmetry scope explicitly. Local `N=4` Bernal probes with `GrapheneTBParameters.preset("4lg").replace(U=0.0)` still show `K/K'` splitting, so higher-even-layer Bernal valley-degeneracy assertions remain deferred pending source-backed reconciliation. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py`.
- [x] Land the first clean-subspace ABA trilayer LL-structure regression in the fast validation tier. Completed (2026-03-31): `tests/test_physics_validation.py` now uses the reusable trilayer mirror block unitary to transform the clean `n_layers=3` Bernal LL Hamiltonian and verifies that the odd block matches a monolayer LL Hamiltonian while the even block matches a bilayer-like LL Hamiltonian with `sqrt(2) * gamma1`, across both valleys. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_symmetry.py tests/test_models.py tests/test_bernal.py`.
- [x] Land the first full-parameter ABA trilayer LL mirror-parity regression in the fast validation tier. Completed (2026-03-31): `tests/test_physics_validation.py` now transforms the realistic ABA trilayer LL Hamiltonian with the reusable mirror block unitary and verifies exact odd/even block decoupling at `U=0`, spectral reconstruction from the separated blocks, and strong recoupling once finite `U` breaks mirror symmetry. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_symmetry.py tests/test_models.py tests/test_bernal.py`.
- [x] Add the minimal ABA trilayer mirror/parity helper surface needed for paper-backed block-decoupling tests. Completed (2026-03-30): `src/contimod_graphene/basis.py` now exposes an explicit ABA trilayer mirror-basis operator, zero-field unitary, generic layer/block unitaries, and odd/even projectors, and `tests/test_physics_validation.py` uses them to verify exact odd/even block decoupling for trilayer Bernal graphene at `U=0` and recoupling once finite `U` breaks mirror symmetry. Validation: `python -m py_compile src/contimod_graphene/basis.py tests/test_physics_validation.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_symmetry.py tests/test_models.py tests/test_bernal.py`.
- [x] Land the first paper-backed ABA trilayer branch-content validation in the explicit mirror basis. Completed (2026-03-30): `tests/test_physics_validation.py` now checks that the clean odd mirror sector matches a monolayer Dirac block while the even sector matches the bilayer-like spectrum with `sqrt(2) * gamma1`, and `PHYSICS_VALIDATION_PLAN.md` records that these checks now live in the fast validation tier. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_symmetry.py tests/test_models.py tests/test_bernal.py`.
- [x] Land the first paper-backed Hamiltonian validation wave in tracked docs/tests: zero-field basis label/projector helpers, BLG clean-spectrum and reduction checks, bilayer AB/ABC equivalence in the LL builder, and ABC outer-site / low-energy-scaling checks. Completed (2026-03-30): added `PHYSICS_VALIDATION_PLAN.md`, zero-field basis helper APIs in `src/contimod_graphene/basis.py`, and `tests/test_physics_validation.py` covering BLG clean exact bands, low-energy two-band agreement, bilayer AB/ABC spectral equivalence, spinless BLG LL zero modes, and ABC trilayer/tetralayer outer-site plus `k^N` scaling checks. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_physics_validation.py tests/test_symmetry.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py`.
- [x] Resolve the Bernal asymmetry-surface inconsistency before adding finite-`Delta` paper-backed tests: `src/contimod_graphene/bernal.py` now applies `Delta` as an A/B sublattice asymmetry across the full Bernal zero-field and LL surfaces, matching the existing bilayer two-band convention. Completed (2026-03-30): added focused BLG asymmetry regressions in `tests/test_physics_validation.py` and a convention pin in `tests/test_bernal.py`, and updated `PHYSICS_VALIDATION_PLAN.md` to document the chosen `Delta` meaning. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_symmetry.py tests/test_models.py tests/test_rhombohedral.py tests/test_bernal.py tests/test_physics_validation.py`.
- [x] Decide whether `Delta` should remain part of the rhombohedral parameter surface, be implemented there, or be documented as unused. Completed (2026-03-30): `Delta` now has a consistent ABC meaning across the zero-field, projected two-band, and LL kernels. For trilayers it matches the literature `Δ2` middle-layer offset relative to the average outer-layer energy; for `N>3` it is extended as a single inversion-even layer-curvature profile. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_symmetry.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py tests/test_physics_validation.py`.
- [x] Decide whether the rhombohedral `delta` slot should remain part of the public parameter surface, be implemented from an explicit source-backed onsite term, or be documented as intentionally unused. Completed (2026-03-30): ABC kernels now document `delta` as intentionally unused for shared-parameter compatibility, and regression tests pin that it does not affect rhombohedral zero-field, two-band, or LL surfaces. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_symmetry.py tests/test_models.py tests/test_bernal.py tests/test_rhombohedral.py tests/test_physics_validation.py`.
- [x] Audit the maintained standalone docs/examples/figure-generation path now that rhombohedral `Delta` is physically active in the ABC/TLG preset. Completed (2026-03-30): `examples/standalone_quickstart.py`, `examples/standalone_gallery.py`, `docs/usage.md`, `docs/examples.md`, and `tests/test_docs_examples.py` now either pin `Delta=0.0` when illustrating a simple `U`-only ABC workflow or explicitly document that the built-in TLG preset carries nonzero `U` and `Delta`. Validation: `python -m py_compile examples/standalone_quickstart.py examples/standalone_gallery.py docs/generate_example_figures.py` plus `JAX_PLATFORMS=cpu pytest -ra tests/test_docs_examples.py tests/test_rhombohedral.py tests/test_physics_validation.py tests/test_models.py`.
- [ ] Reconcile the README quickstart ABC snippet with the maintained standalone examples once the pre-existing README edits are resolved. The tracked examples/docs now pin `Delta=0.0` for `U`-only bias demonstrations, but the currently dirty README quickstart still shows `.replace(U=20.0)` without that clarification.
- [x] Reconcile the Bernal four-layer LL valley-degeneracy expectation with the current implementation before asserting it in CI. Completed (2026-03-31): the source-backed review traced the apparent `K/K'` splitting to a `W/W'` parity bug in `src/contimod_graphene/bernal.py`, not to the `4lg` preset itself. The ABAB four-layer LL degeneracy is now covered in `tests/test_physics_validation.py`, and the zero-field `1 -> 3` / `2 -> 4` block swap is pinned in `tests/test_bernal.py`. Validation: `JAX_PLATFORMS=cpu pytest -ra tests/test_bernal.py tests/test_physics_validation.py tests/test_models.py tests/test_rhombohedral.py`.
- [x] Refresh the older companion notebooks under `examples/` to match the maintained standalone API surface where appropriate. Completed (2026-03-30): `bandstructure_plots.ipynb`, `bernal_bands_LL.ipynb`, and `landau_level_fans.ipynb` now use the public API directly, while `contimod_example.ipynb` remains explicitly downstream integration and is tested as such in `tests/test_docs_examples.py`.

## Blocked / Needs Decision
- [ ] Decide how much of the existing `contimod` wrapper surface, if any, should move into `contimod_graphene` versus remaining as downstream integration code.

## Seed Notes
- The source tree is already mostly self-contained and does not depend on `contimod` in core modules, which supports the standalone package positioning.
- The remaining follow-on work is less about whether this is a real package and more about how polished and complete that standalone surface should become.
